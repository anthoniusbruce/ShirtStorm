@using System.ComponentModel.DataAnnotations
@model CommissionViewModel

@{
	var parms = new Dictionary<string, string>
			{
				{ "addressType", "Commissions" }
			};
}

<div class="data-entry col-sm-11">
	<form asp-controller="UpcomingDesigns" asp-action="CommissionUpdateBlock" method="post" enctype="multipart/form-data">
		@Html.AntiForgeryToken()
		<input asp-for="@Model.Id" />
		<input asp-for="@Model.CreatedDate" />
		<div class="address-create-edit-row">
			<div class="address-create-edit-field">
				@Html.TextArea("Description", new { @placeholder = "Type description here...", @class = "form-control", @required = "required" })
			</div>
		</div>
		<div class="far-right">
			<a class="btn btn-default" asp-controller="UpcomingDesigns" asp-action="Cancel" asp-all-route-data="@parms">Cancel</a>
			<button class="btn btn-success relative" id="btnSubmit" type="submit">Submit</button>
		</div>
	</form>
</div>
