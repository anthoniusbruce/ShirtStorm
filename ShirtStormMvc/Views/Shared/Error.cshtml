@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
    Layout = "EmptyLayout";
}

@await Component.InvokeAsync("Preamble")

@if (Model.ShowRequestId)
{
    <p>
        <code>@Model.RequestId</code>
    </p>
}
